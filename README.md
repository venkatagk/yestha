# yestha.com

Public site for Yestha Inc. (static HTML + assets).

## Local preview

- Open `index.html` in a browser, or run a simple static server:
  - PowerShell: `python -m http.server 8000`
  - Then visit: `http://localhost:8000`
